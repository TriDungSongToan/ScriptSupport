<Window x:Class="ScriptSupport.MainWindowStandalone"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:ScriptSupport"
        xmlns:UserControls="clr-namespace:ScriptSupport.UserControls"
        xmlns:ViewModels="clr-namespace:ScriptSupport.ViewModels"
        xmlns:Converters="clr-namespace:ScriptSupport.Converters"
        xmlns:States="clr-namespace:ScriptSupport.States"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800" >

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="30" ResizeBorderThickness="6"
                            CornerRadius="10" GlassFrameThickness="0"
                            UseAeroCaptionButtons="True" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="TitleButton" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{Binding UIConfig.Foreground}" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{Binding UIConfig.ThemeColor}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource TitleButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="BorderRoot" FlowDirection="{Binding UIConfig.FlowDirection}" d:FlowDirection="LeftToRight"
            Background="Transparent" CornerRadius="10">

        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Visibility="Visible"
                  Background="{Binding UIConfig.Background}"
                  Height="30" MouseDown="TitleBar_MouseDown" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True"
                            Orientation="Horizontal" >
                    <Image Source="/Images/Logo.ico" Width="auto" Height="auto" Margin="6,4,0,4"
                           VerticalAlignment="Center" />
                    <TextBlock Text="Script Support" VerticalAlignment="Center" FontWeight="SemiBold" Margin="8,0"
                               Foreground="{Binding UIConfig.Foreground}" />
                </StackPanel>
                
                <StackPanel Grid.Column="1" Margin="0"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True"
                            Orientation="Horizontal"  HorizontalAlignment="Right">
                    <Button Content="—" Style="{StaticResource TitleButton}" Click="Minimize_Click"/>
                    <Button Content="▢" Style="{StaticResource TitleButton}" Click="Maximize_Click"/>
                    <Button Content="X" Style="{StaticResource CloseButton}" Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" >
                <ContentPresenter x:Name="MainContent" />
            </Grid>
        </Grid>
    </Border>
</Window>
